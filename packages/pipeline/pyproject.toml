[project]
name = "market-lens-pipeline"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "pandas",
    "yfinance",
    "python-dotenv",
    "pandas-market-calendars",
    "market-lens-utils",
]

[project.scripts]
pipeline = "market_lens_pipeline.pipeline:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.uv.sources]
market-lens-utils = { workspace = true }
